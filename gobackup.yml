web:
  host: 0.0.0.0
models:
  my_app:
    schedule:
      cron: "* * * * *"
    compress_with:
      type: tgz
    storages:
      local:
        type: local
        keep: 20
        path: /data/backups
    databases:
      my_app:
        database: fkm_time
        type: postgresql
        host: database
        port: 5432
        username: root
        password: password
